import {TextField} from '@mui/material';
import * as React from 'react';

const ProfileFormInput = (props) => {
  return (<TextField sx={{marginTop: '22px', width: 1}} {...props} />);
};

export default ProfileFormInput;
